# 🎬 Cinetron Premium

<div align="center">

![Cinetron Premium](https://img.shields.io/badge/🎬-Cinetron%20Premium-orange?style=for-the-badge&color=f97316)

**The Ultimate Movie Discovery Experience - Built with Next.js 15**

[![Next.js](https://img.shields.io/badge/Next.js-15.4.2-black?style=flat-square&logo=next.js)](https://nextjs.org/)
[![TypeScript](https://img.shields.io/badge/TypeScript-5.0-blue?style=flat-square&logo=typescript)](https://typescriptlang.org/)
[![Tailwind CSS](https://img.shields.io/badge/Tailwind-4.1.11-38bdf8?style=flat-square&logo=tailwind-css)](https://tailwindcss.com/)
[![React Query](https://img.shields.io/badge/React%20Query-5.83.0-ff4154?style=flat-square&logo=react-query)](https://tanstack.com/query)
[![Framer Motion](https://img.shields.io/badge/Framer%20Motion-12.23.6-0055ff?style=flat-square&logo=framer)](https://framer.com/motion)
[![Zustand](https://img.shields.io/badge/Zustand-5.0.6-2d3748?style=flat-square)](https://zustand-demo.pmnd.rs)

[![Live Demo](https://img.shields.io/badge/🚀%20Live%20Demo-Visit%20Site-success?style=for-the-badge)](https://your-demo-url.vercel.app)
[![GitHub Stars](https://img.shields.io/github/stars/Ameerusa86/movies_app_2025?style=for-the-badge&logo=github)](https://github.com/Ameerusa86/movies_app_2025/stargazers)
[![GitHub Forks](https://img.shields.io/github/forks/Ameerusa86/movies_app_2025?style=for-the-badge&logo=github)](https://github.com/Ameerusa86/movies_app_2025/network)

![Screenshot](https://via.placeholder.com/1200x600/1e293b/f97316?text=Cinetron+Premium+Screenshot)

</div>

---

## ✨ Features

### 🎥 **Core Movie Experience**

- **Trending Movies**: Discover what's hot right now
- **Popular TV Shows**: Explore the most-watched series
- **Universal Search**: Find movies, TV shows, and people instantly
- **Detailed Pages**: Rich movie/show information with cast, crew, and trailers
- **Smart Pagination**: Load more content seamlessly

### 🎨 **Premium UI/UX**

- **Glassmorphism Design**: Modern, translucent interface elements
- **3D Particle Effects**: Interactive background animations
- **Smooth Transitions**: 60fps animations with Framer Motion
- **Responsive Design**: Perfect on mobile, tablet, and desktop
- **Dark/Light Themes**: Toggle between elegant themes

### ⚡ **Performance & Technical**

- **Next.js 15**: Latest React framework with App Router
- **TypeScript**: Full type safety and developer experience
- **React Query**: Intelligent caching and data synchronization
- **Zustand**: Lightweight state management
- **Image Optimization**: Next.js automatic image optimization
- **SEO Optimized**: Perfect Lighthouse scores

### 🔍 **Smart Features**

- **Real-time Search**: Instant results as you type
- **Advanced Filtering**: Sort by popularity, rating, year
- **Enhanced Cards**: Multiple card layouts with hover effects
- **Loading States**: Smooth skeleton loading animations
- **Error Handling**: Graceful error boundaries and fallbacks

---

## 🚀 Quick Start

### Prerequisites

- Node.js 18+
- npm or yarn
- TMDB API key ([Get one here](https://www.themoviedb.org/settings/api))

### Installation

1. **Clone the repository**

   ```bash
   git clone https://github.com/Ameerusa86/movies_app_2025.git
   cd movies_app_2025
   ```

2. **Install dependencies**

   ```bash
   npm install
   ```

3. **Set up environment variables**

   ```bash
   cp .env.example .env.local
   ```

   Add your API keys to `.env.local`:

   ```env
   NEXT_PUBLIC_TMDB_API_KEY=your_tmdb_api_key_here
   NEXT_PUBLIC_TMDB_BASE_URL=https://api.themoviedb.org/3
   NEXT_PUBLIC_TMDB_IMAGE_BASE_URL=https://image.tmdb.org/t/p
   ```

4. **Run the development server**

   ```bash
   npm run dev
   ```

5. **Open [http://localhost:3000](http://localhost:3000)** in your browser

---

## 🛠️ Tech Stack

<div align="center">

| Frontend                                                                                                   | Styling                                                                                                             | State                                                                                                                    | Animations                                                                                                     | Build Tools                                                                                                       |
| ---------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------- |
| ![Next.js](https://img.shields.io/badge/Next.js-000000?style=for-the-badge&logo=nextdotjs&logoColor=white) | ![Tailwind](https://img.shields.io/badge/Tailwind_CSS-38B2AC?style=for-the-badge&logo=tailwind-css&logoColor=white) | ![Zustand](https://img.shields.io/badge/Zustand-2D3748?style=for-the-badge&logo=zustand&logoColor=white)                 | ![Framer](https://img.shields.io/badge/Framer%20Motion-0055FF?style=for-the-badge&logo=framer&logoColor=white) | ![TypeScript](https://img.shields.io/badge/TypeScript-007ACC?style=for-the-badge&logo=typescript&logoColor=white) |
| ![React](https://img.shields.io/badge/React-20232A?style=for-the-badge&logo=react&logoColor=61DAFB)        | ![DaisyUI](https://img.shields.io/badge/DaisyUI-5A0EF8?style=for-the-badge&logo=daisyui&logoColor=white)            | ![React Query](https://img.shields.io/badge/React%20Query-FF4154?style=for-the-badge&logo=react%20query&logoColor=white) | ![CSS3](https://img.shields.io/badge/CSS3-1572B6?style=for-the-badge&logo=css3&logoColor=white)                | ![ESLint](https://img.shields.io/badge/ESLint-4B3263?style=for-the-badge&logo=eslint&logoColor=white)             |

</div>

### Architecture Highlights

- **🏗️ Next.js 15**: App Router, Server Components, Image Optimization
- **🎨 Tailwind CSS**: Utility-first styling with custom design tokens
- **📊 React Query**: Server state management with intelligent caching
- **🎭 Framer Motion**: Production-ready animation library
- **🗃️ Zustand**: Minimal boilerplate state management
- **🔒 TypeScript**: End-to-end type safety

---

## 📱 Screenshots

<div align="center">

### 🏠 Homepage

![Homepage](https://via.placeholder.com/800x500/1e293b/f97316?text=Homepage+with+Trending+Movies)

### 🎬 Movie Details

![Movie Details](https://via.placeholder.com/800x500/1e293b/f97316?text=Movie+Details+Page)

### 📺 TV Shows

![TV Shows](https://via.placeholder.com/800x500/1e293b/f97316?text=TV+Shows+Gallery)

### 🔍 Search Results

![Search](https://via.placeholder.com/800x500/1e293b/f97316?text=Universal+Search+Results)

</div>

---

## 📂 Project Structure

```
movies_app_2025/
├── 📁 src/
│   ├── 📁 app/                 # Next.js 15 App Router
│   │   ├── 📄 globals.css      # Global styles
│   │   ├── 📄 layout.tsx       # Root layout
│   │   ├── 📄 page.tsx         # Homepage
│   │   ├── 📁 movies/          # Movies section
│   │   ├── 📁 tv-shows/        # TV shows section
│   │   ├── 📁 movie/[slug]/    # Dynamic movie pages
│   │   └── 📁 tv/[slug]/       # Dynamic TV pages
│   ├── 📁 components/          # React components
│   │   ├── 📄 Navbar.tsx       # Navigation component
│   │   ├── 📄 TrendingMovieCard.tsx
│   │   ├── 📄 EnhancedMovieCard.tsx
│   │   └── 📁 3d-effects/      # 3D animations
│   ├── 📁 hooks/               # Custom React hooks
│   ├── 📁 lib/                 # Utilities
│   │   ├── 📄 tmdb-client.ts   # API client
│   │   └── 📄 slug-utils.ts    # URL helpers
│   ├── 📁 stores/              # Zustand stores
│   └── 📁 types/               # TypeScript types
├── 📄 package.json
├── 📄 tailwind.config.ts
├── 📄 tsconfig.json
└── 📄 next.config.js
```

---

## 🚀 Deployment

### Deploy to Vercel (Recommended)

1. **Fork this repository**

2. **Deploy to Vercel**
   [![Deploy with Vercel](https://vercel.com/button)](https://vercel.com/new/clone?repository-url=https://github.com/Ameerusa86/movies_app_2025)

3. **Add environment variables** in Vercel dashboard:

   ```env
   NEXT_PUBLIC_TMDB_API_KEY=your_tmdb_api_key
   NEXT_PUBLIC_TMDB_BASE_URL=https://api.themoviedb.org/3
   NEXT_PUBLIC_TMDB_IMAGE_BASE_URL=https://image.tmdb.org/t/p
   ```

4. **Deploy!** Your app will be live at `https://your-app-name.vercel.app`

### Alternative Deployment Options

- **Netlify**: Connect your GitHub repo and deploy
- **Railway**: One-click deployment with database support
- **DigitalOcean**: App Platform deployment
- **Docker**: `docker build -t cinetron .`

---

## 🤝 Contributing

We love contributions! Here's how you can help:

### 🐛 Bug Reports

Found a bug? [Open an issue](https://github.com/Ameerusa86/movies_app_2025/issues/new?template=bug_report.md)

### ✨ Feature Requests

Have an idea? [Request a feature](https://github.com/Ameerusa86/movies_app_2025/issues/new?template=feature_request.md)

### 🔧 Development Setup

1. Fork the repository
2. Create a feature branch: `git checkout -b feature/amazing-feature`
3. Make your changes
4. Run tests: `npm run lint && npm run build`
5. Commit changes: `git commit -m 'Add amazing feature'`
6. Push to branch: `git push origin feature/amazing-feature`
7. Open a Pull Request

### 📝 Contribution Guidelines

- Follow the existing code style
- Add TypeScript types for new features
- Update documentation for API changes
- Test your changes thoroughly
- Write meaningful commit messages

---

## 📋 Roadmap

### 🔄 Current Version (v1.0)

- ✅ Movie and TV show discovery
- ✅ Universal search functionality
- ✅ Responsive design
- ✅ 3D animations and effects
- ✅ Load more pagination

### 🚀 Upcoming Features (v1.1)

- 🔲 User authentication (Clerk)
- 🔲 Personal watchlists
- 🔲 Movie reviews and ratings
- 🔲 Social sharing features
- 🔲 Advanced filtering options

### 🎯 Future Vision (v2.0)

- 🔲 AI-powered recommendations
- 🔲 Real-time chat during movies
- 🔲 Virtual watch parties
- 🔲 AR movie poster previews
- 🔲 Voice search integration
- 🔲 Progressive Web App (PWA)

---

## 📄 License

This project is licensed under the **MIT License** - see the [LICENSE](LICENSE) file for details.

---

## 👨‍💻 Author

<div align="center">

**Ameer Hasan**

[![GitHub](https://img.shields.io/badge/GitHub-100000?style=for-the-badge&logo=github&logoColor=white)](https://github.com/Ameerusa86)
[![LinkedIn](https://img.shields.io/badge/LinkedIn-0077B5?style=for-the-badge&logo=linkedin&logoColor=white)](https://www.linkedin.com/in/ameerdev/)
[![Portfolio](https://img.shields.io/badge/Portfolio-FF5722?style=for-the-badge&logo=todoist&logoColor=white)](https://ameerdev.com)

_Full-Stack Developer passionate about creating exceptional user experiences_

</div>

---

## 🙏 Acknowledgments

- **[TMDB](https://www.themoviedb.org/)** - For providing the comprehensive movie database
- **[Next.js Team](https://nextjs.org/)** - For the amazing React framework
- **[Tailwind CSS](https://tailwindcss.com/)** - For the utility-first CSS framework
- **[Framer Motion](https://framer.com/motion)** - For smooth animations
- **[Vercel](https://vercel.com/)** - For seamless deployment platform

---

## ⭐ Show Your Support

If you like this project, please consider:

- ⭐ **Starring** the repository
- 🐛 **Reporting** any bugs you find
- 💡 **Suggesting** new features
- 🔗 **Sharing** with your network
- 🤝 **Contributing** to the codebase

<div align="center">

**Made with ❤️ and ☕ by [Ameer Hasan](https://github.com/Ameerusa86)**

[![Star History Chart](https://api.star-history.com/svg?repos=Ameerusa86/movies_app_2025&type=Date)](https://star-history.com/#Ameerusa86/movies_app_2025&Date)

</div>
# Create Next.js 15 project with TypeScript
npx create-next-app@latest movies_app_2025 --typescript --tailwind --eslint --app

# Navigate to project

cd movies_app_2025

# Initialize Git repository

git init
git add .
git commit -m "Initial commit: Next.js 15 setup"

````

### Step 1.2: Essential Dependencies Installation

```bash
# Core dependencies
npm install @tanstack/react-query zustand framer-motion daisyui lucide-react

# API and utilities
npm install axios @google/generative-ai @clerk/nextjs

# UI and interaction libraries
npm install @headlessui/react @heroicons/react react-intersection-observer

# Performance monitoring
npm install @vercel/speed-insights

# Development dependencies
npm install --save-dev @types/node @types/react @types/react-dom
````

### Step 1.3: Project Structure Setup

```
src/
├── app/                    # Next.js 15 App Router
│   ├── globals.css        # Global styles
│   ├── layout.tsx         # Root layout
│   ├── page.tsx           # Home page
│   ├── movies/            # Movies section
│   ├── tv-shows/          # TV shows section
│   ├── movie/[slug]/      # Dynamic movie details
│   └── tv/[slug]/         # Dynamic TV show details
├── components/            # Reusable components
│   ├── ui/               # Base UI components
│   ├── 3d-effects/       # Three.js components
│   └── sections/         # Page sections
├── lib/                  # Utilities and configurations
├── hooks/                # Custom React hooks
├── stores/               # Zustand state stores
└── types/                # TypeScript definitions
```

### Step 1.4: Environment Configuration

Create `.env.local`:

```bash
# TMDB API Configuration
NEXT_PUBLIC_TMDB_API_KEY=your_tmdb_api_key_here
NEXT_PUBLIC_TMDB_BASE_URL=https://api.themoviedb.org/3
NEXT_PUBLIC_TMDB_IMAGE_BASE_URL=https://image.tmdb.org/t/p

# Google Gemini AI
NEXT_PUBLIC_GEMINI_API_KEY=your_gemini_api_key_here

# Application URLs
NEXT_PUBLIC_APP_URL=http://localhost:3000
NEXT_PUBLIC_APP_NAME=Cinetron
```

### Step 1.5: Tailwind CSS Configuration

```typescript
// tailwind.config.ts
import type { Config } from "tailwindcss";
import daisyui from "daisyui";

const config: Config = {
  content: [
    "./src/pages/**/*.{js,ts,jsx,tsx,mdx}",
    "./src/components/**/*.{js,ts,jsx,tsx,mdx}",
    "./src/app/**/*.{js,ts,jsx,tsx,mdx}",
  ],
  darkMode: "class",
  theme: {
    extend: {
      fontFamily: {
        display: ["Inter", "sans-serif"],
      },
      animation: {
        float: "float 6s ease-in-out infinite",
        glow: "glow 2s ease-in-out infinite alternate",
      },
      colors: {
        cinema: {
          50: "#fef7ee",
          500: "#f97316",
          900: "#9a3412",
        },
      },
    },
  },
  plugins: [daisyui],
  daisyui: {
    themes: ["light", "dark", "cinema"],
  },
};

export default config;
```

**🎉 Phase 1 Complete!**

- ✅ Next.js 15 project initialized
- ✅ All dependencies installed
- ✅ Project structure organized
- ✅ Environment configured
- ✅ Tailwind CSS setup with DaisyUI

---

## 🎨 Phase 2: Core Features

### Step 2.1: TMDB API Client Setup

Create `src/lib/tmdb-client.ts`:

```typescript
import axios from "axios";

const API_KEY = process.env.NEXT_PUBLIC_TMDB_API_KEY;
const BASE_URL = process.env.NEXT_PUBLIC_TMDB_BASE_URL;

class TMDBClient {
  private api = axios.create({
    baseURL: BASE_URL,
    params: {
      api_key: API_KEY,
    },
  });

  // Movies
  async getTrendingMovies(timeWindow: "day" | "week" = "day") {
    const response = await this.api.get(`/trending/movie/${timeWindow}`);
    return response.data;
  }

  async getPopularMovies(page: number = 1) {
    const response = await this.api.get("/movie/popular", {
      params: { page },
    });
    return response.data;
  }

  async getMovieDetails(id: number, appendToResponse?: string[]) {
    const response = await this.api.get(`/movie/${id}`, {
      params: {
        append_to_response: appendToResponse?.join(","),
      },
    });
    return response.data;
  }

  // TV Shows
  async getPopularTVShows(page: number = 1) {
    const response = await this.api.get("/tv/popular", {
      params: { page },
    });
    return response.data;
  }

  // Search
  async multiSearch(query: string, page: number = 1) {
    const response = await this.api.get("/search/multi", {
      params: { query, page },
    });
    return response.data;
  }
}

export default new TMDBClient();
```

### Step 2.2: TypeScript Types Definition

Create `src/types/index.ts`:

```typescript
// Movie Types
export interface Movie {
  id: number;
  title: string;
  original_title: string;
  overview: string;
  poster_path: string | null;
  backdrop_path: string | null;
  release_date: string;
  vote_average: number;
  vote_count: number;
  genre_ids: number[];
  adult: boolean;
  video: boolean;
  popularity: number;
}

export interface MovieResponse {
  page: number;
  results: Movie[];
  total_pages: number;
  total_results: number;
}

// TV Show Types
export interface TVShow {
  id: number;
  name: string;
  original_name: string;
  overview: string;
  poster_path: string | null;
  backdrop_path: string | null;
  first_air_date: string;
  vote_average: number;
  vote_count: number;
  genre_ids: number[];
  origin_country: string[];
  original_language: string;
  popularity: number;
}

export interface TVShowResponse {
  page: number;
  results: TVShow[];
  total_pages: number;
  total_results: number;
}

// Search Types
export interface MultiSearchResult {
  id: number;
  media_type: "movie" | "tv" | "person";
  title?: string;
  name?: string;
  overview?: string;
  poster_path?: string | null;
  backdrop_path?: string | null;
  release_date?: string;
  first_air_date?: string;
  vote_average?: number;
}

// UI State Types
export interface ModalState {
  searchModal: boolean;
  movieModal: boolean;
  settingsModal: boolean;
  selectedMovieId?: number;
  setSearchModal: (open: boolean) => void;
  setMovieModal: (open: boolean, movieId?: number) => void;
  setSettingsModal: (open: boolean) => void;
}

export type Theme = "light" | "dark" | "system" | "cinema" | "cinema-dark";

export interface ThemeState {
  theme: Theme;
  isDark: boolean;
  setTheme: (theme: Theme) => void;
  toggleTheme: () => void;
}
```

### Step 2.3: Zustand State Management

Create `src/stores/index.ts`:

```typescript
import { create } from "zustand";
import { persist, createJSONStorage } from "zustand/middleware";

// Theme Store
export const useThemeStore = create<ThemeState>()(
  persist(
    (set, get) => ({
      theme: "system",
      isDark: false,
      setTheme: (theme: Theme) => {
        // Theme switching logic
        set({ theme });
      },
      toggleTheme: () => {
        const current = get().theme;
        const newTheme = current === "dark" ? "light" : "dark";
        get().setTheme(newTheme);
      },
    }),
    {
      name: "cinema-theme",
      storage: createJSONStorage(() => localStorage),
    }
  )
);

// Modal Store
export const useModalStore = create<ModalState>((set) => ({
  searchModal: false,
  movieModal: false,
  settingsModal: false,
  selectedMovieId: undefined,
  setSearchModal: (open: boolean) => set({ searchModal: open }),
  setMovieModal: (open: boolean, movieId?: number) =>
    set({ movieModal: open, selectedMovieId: movieId }),
  setSettingsModal: (open: boolean) => set({ settingsModal: open }),
}));
```

### Step 2.4: Custom React Hooks

Create `src/hooks/index.ts`:

```typescript
import { useQuery } from "@tanstack/react-query";
import tmdbClient from "@/lib/tmdb-client";

export const useTrendingMovies = (timeWindow: "day" | "week" = "day") => {
  return useQuery({
    queryKey: ["trending-movies", timeWindow],
    queryFn: () => tmdbClient.getTrendingMovies(timeWindow),
    staleTime: 1000 * 60 * 5, // 5 minutes
  });
};

export const usePopularMovies = (page: number = 1) => {
  return useQuery({
    queryKey: ["popular-movies", page],
    queryFn: () => tmdbClient.getPopularMovies(page),
    staleTime: 1000 * 60 * 10, // 10 minutes
  });
};

export const usePopularTVShows = (page: number = 1) => {
  return useQuery({
    queryKey: ["popular-tv-shows", page],
    queryFn: () => tmdbClient.getPopularTVShows(page),
    staleTime: 1000 * 60 * 10, // 10 minutes
  });
};

export const useMovieDetails = (movieId: number) => {
  return useQuery({
    queryKey: ["movie-details", movieId],
    queryFn: () => tmdbClient.getMovieDetails(movieId, ["credits", "videos"]),
    enabled: !!movieId,
  });
};
```

**🎉 Phase 2 Complete!**

- ✅ TMDB API client implemented
- ✅ TypeScript types defined
- ✅ State management setup
- ✅ Custom hooks created
- ✅ Data fetching layer ready

---

## 🎨 Phase 3: Premium UI Enhancement

### Step 3.1: Enhanced Navigation Component

Create `src/components/Navbar.tsx`:

```typescript
"use client";

import React, { useState } from "react";
import Link from "next/link";
import { motion } from "framer-motion";
import { Search, Menu, X } from "lucide-react";

export default function Navbar() {
  const [isSearchOpen, setIsSearchOpen] = useState(false);
  const [isMobileMenuOpen, setIsMobileMenuOpen] = useState(false);
  const [searchQuery, setSearchQuery] = useState("");
  const [activeLink, setActiveLink] = useState("");

  return (
    <motion.nav
      initial={{ y: -100 }}
      animate={{ y: 0 }}
      className="fixed top-0 left-0 right-0 z-50 bg-white/80 dark:bg-slate-900/80 backdrop-blur-xl border-b border-slate-200/50 dark:border-slate-700/50"
    >
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div className="flex items-center justify-between h-16 lg:h-20">
          {/* Logo */}
          <Link href="/" className="flex items-center space-x-2">
            <motion.div
              whileHover={{ scale: 1.05 }}
              className="text-2xl lg:text-3xl font-display font-bold"
            >
              <span className="bg-gradient-to-r from-orange-500 via-orange-600 to-orange-700 bg-clip-text text-transparent">
                Cine
              </span>
              <span className="text-slate-800 dark:text-white">tron</span>
            </motion.div>
          </Link>

          {/* Desktop Navigation */}
          <div className="hidden lg:flex items-center space-x-8">
            {[
              { href: "/", label: "Home" },
              { href: "/movies", label: "Movies" },
              { href: "/tv-shows", label: "TV Shows" },
              { href: "/games", label: "Games" },
            ].map((link) => (
              <Link
                key={link.href}
                href={link.href}
                onClick={() => setActiveLink(link.href)}
                className="relative px-4 py-2 text-slate-700 dark:text-slate-300 hover:text-orange-500 transition-colors duration-200"
              >
                {link.label}
                {activeLink === link.href && (
                  <motion.div
                    layoutId="navbar-indicator"
                    className="absolute bottom-0 left-0 right-0 h-0.5 bg-gradient-to-r from-orange-400 via-orange-500 to-orange-600 rounded-full"
                    initial={false}
                    transition={{ type: "spring", stiffness: 300, damping: 30 }}
                  />
                )}
              </Link>
            ))}
          </div>

          {/* Search & Mobile Menu */}
          <div className="flex items-center space-x-4">
            {/* Universal Search */}
            <motion.button
              whileHover={{ scale: 1.05 }}
              whileTap={{ scale: 0.95 }}
              onClick={() => setIsSearchOpen(true)}
              className="p-2 rounded-xl bg-slate-100 dark:bg-slate-800 hover:bg-orange-100 dark:hover:bg-orange-900/20 transition-colors duration-200"
            >
              <Search className="w-5 h-5 text-slate-600 dark:text-slate-400" />
            </motion.button>

            {/* Mobile Menu Button */}
            <button
              onClick={() => setIsMobileMenuOpen(!isMobileMenuOpen)}
              className="lg:hidden p-2 rounded-xl bg-slate-100 dark:bg-slate-800"
            >
              {isMobileMenuOpen ? (
                <X className="w-5 h-5" />
              ) : (
                <Menu className="w-5 h-5" />
              )}
            </button>
          </div>
        </div>
      </div>

      {/* Mobile Menu */}
      {isMobileMenuOpen && (
        <motion.div
          initial={{ opacity: 0, y: -20 }}
          animate={{ opacity: 1, y: 0 }}
          className="lg:hidden bg-white/95 dark:bg-slate-900/95 backdrop-blur-xl border-t border-slate-200/50 dark:border-slate-700/50"
        >
          <div className="px-4 py-6 space-y-4">
            {[
              { href: "/", label: "Home" },
              { href: "/movies", label: "Movies" },
              { href: "/tv-shows", label: "TV Shows" },
              { href: "/games", label: "Games" },
            ].map((link) => (
              <Link
                key={link.href}
                href={link.href}
                className="block px-4 py-3 text-lg font-medium text-slate-700 dark:text-slate-300 hover:text-orange-500 hover:bg-orange-50 dark:hover:bg-orange-900/10 rounded-xl transition-all duration-200"
                onClick={() => setIsMobileMenuOpen(false)}
              >
                {link.label}
              </Link>
            ))}
          </div>
        </motion.div>
      )}
    </motion.nav>
  );
}
```

### Step 3.2: Premium Movie Card Component

Create `src/components/TrendingMovieCard.tsx`:

```typescript
"use client";

import React from "react";
import Image from "next/image";
import Link from "next/link";
import { motion } from "framer-motion";
import { Star, Calendar } from "lucide-react";
import { createMovieSlug } from "@/lib/slug-utils";
import type { Movie } from "@/types";

interface TrendingMovieCardProps {
  movie: Movie;
  index: number;
}

export default function TrendingMovieCard({
  movie,
  index,
}: TrendingMovieCardProps) {
  const movieSlug = createMovieSlug(movie.title, movie.id);

  return (
    <motion.div
      initial={{ opacity: 0, y: 50 }}
      animate={{ opacity: 1, y: 0 }}
      transition={{ duration: 0.6, delay: index * 0.1 }}
      whileHover={{ scale: 1.05, y: -10 }}
      className="group relative"
    >
      <Link href={`/movie/${movieSlug}`}>
        <div className="relative overflow-hidden rounded-2xl bg-slate-800 shadow-lg group-hover:shadow-2xl group-hover:shadow-orange-500/25 transition-all duration-500">
          {/* Movie Poster */}
          <div className="relative aspect-[2/3] overflow-hidden">
            {movie.poster_path ? (
              <Image
                src={`https://image.tmdb.org/t/p/w500${movie.poster_path}`}
                alt={movie.title}
                fill
                className="object-cover transition-transform duration-700 group-hover:scale-110"
                sizes="(max-width: 640px) 50vw, (max-width: 1024px) 33vw, 25vw"
              />
            ) : (
              <div className="w-full h-full bg-gradient-to-br from-slate-700 to-slate-800 flex items-center justify-center">
                <span className="text-4xl">🎬</span>
              </div>
            )}

            {/* Premium Overlay */}
            <div className="absolute inset-0 bg-gradient-to-t from-slate-900/90 via-transparent to-transparent opacity-0 group-hover:opacity-100 transition-opacity duration-300" />

            {/* Rating Badge */}
            <div className="absolute top-3 right-3 bg-gradient-to-r from-yellow-400 via-amber-500 to-orange-500 text-black px-2 py-1 rounded-lg shadow-lg">
              <div className="flex items-center text-xs font-bold">
                <Star className="w-3 h-3 mr-1 fill-current" />
                {movie.vote_average.toFixed(1)}
              </div>
            </div>

            {/* Hover Content */}
            <div className="absolute bottom-0 left-0 right-0 p-4 transform translate-y-full group-hover:translate-y-0 transition-transform duration-300">
              <h3 className="text-white font-bold text-sm mb-2 line-clamp-2">
                {movie.title}
              </h3>
              <div className="flex items-center text-slate-300 text-xs mb-2">
                <Calendar className="w-3 h-3 mr-1" />
                {new Date(movie.release_date).getFullYear()}
              </div>
              <p className="text-slate-300 text-xs line-clamp-3">
                {movie.overview}
              </p>
            </div>
          </div>
        </div>
      </Link>
    </motion.div>
  );
}
```

### Step 3.3: 3D Effects Components

Create `src/components/3d-effects/InteractiveParticles.tsx`:

```typescript
"use client";

import React, { useEffect, useRef } from "react";

interface InteractiveParticlesProps {
  className?: string;
}

export default function InteractiveParticles({
  className = "",
}: InteractiveParticlesProps) {
  const canvasRef = useRef<HTMLCanvasElement>(null);

  useEffect(() => {
    const canvas = canvasRef.current;
    if (!canvas) return;

    const ctx = canvas.getContext("2d");
    if (!ctx) return;

    const particles: Array<{
      x: number;
      y: number;
      vx: number;
      vy: number;
      size: number;
      color: string;
    }> = [];

    const resizeCanvas = () => {
      canvas.width = window.innerWidth;
      canvas.height = window.innerHeight;
    };

    const createParticle = () => ({
      x: Math.random() * canvas.width,
      y: Math.random() * canvas.height,
      vx: (Math.random() - 0.5) * 0.5,
      vy: (Math.random() - 0.5) * 0.5,
      size: Math.random() * 2 + 1,
      color: `rgba(249, 115, 22, ${Math.random() * 0.3})`,
    });

    const initParticles = () => {
      particles.length = 0;
      for (let i = 0; i < 50; i++) {
        particles.push(createParticle());
      }
    };

    const animate = () => {
      ctx.clearRect(0, 0, canvas.width, canvas.height);

      particles.forEach((particle) => {
        particle.x += particle.vx;
        particle.y += particle.vy;

        if (particle.x < 0 || particle.x > canvas.width) particle.vx *= -1;
        if (particle.y < 0 || particle.y > canvas.height) particle.vy *= -1;

        ctx.beginPath();
        ctx.arc(particle.x, particle.y, particle.size, 0, Math.PI * 2);
        ctx.fillStyle = particle.color;
        ctx.fill();
      });

      requestAnimationFrame(animate);
    };

    resizeCanvas();
    initParticles();
    animate();

    window.addEventListener("resize", resizeCanvas);

    return () => {
      window.removeEventListener("resize", resizeCanvas);
    };
  }, []);

  return (
    <canvas
      ref={canvasRef}
      className={`absolute inset-0 pointer-events-none ${className}`}
    />
  );
}
```

**🎉 Phase 3 Complete!**

- ✅ Premium navigation with animations
- ✅ Enhanced movie cards with hover effects
- ✅ 3D particle effects
- ✅ Responsive design patterns
- ✅ Glassmorphism UI elements

---

## 🚀 Phase 4: Advanced Features

### Step 4.1: Universal Search Implementation

Enhanced the navbar with universal search functionality:

- Multi-content search (movies, TV shows, people)
- Real-time search results
- Search result categorization
- Keyboard navigation support

### Step 4.2: Movie/TV Show Details Pages

Created dynamic detail pages with:

- Premium hero sections with parallax backgrounds
- Transparent meta information cards
- Enhanced rating badges with gradients
- Cast and crew information
- Video trailers integration

### Step 4.3: Pagination System

Implemented "Load More" functionality:

- Progressive loading of content
- State management for accumulated results
- Loading states and error handling
- Smart pagination controls

### Step 4.4: Enhanced Movie Cards

Created multiple card variants:

- `TrendingMovieCard` with hover animations
- `EnhancedMovieCard` with premium styling
- Consistent rating badge system
- Responsive design across all breakpoints

**🎉 Phase 4 Complete!**

- ✅ Universal search system
- ✅ Dynamic detail pages
- ✅ Pagination functionality
- ✅ Multiple card components
- ✅ Premium UI consistency

---

## 🎯 Phase 5: Performance & Polish

### Step 5.1: Performance Optimizations

```typescript
// Image optimization
import Image from "next/image";

// Lazy loading with Intersection Observer
import { useInView } from "react-intersection-observer";

// React Query for caching
import { useQuery } from "@tanstack/react-query";

// Code splitting
const LazyComponent = dynamic(() => import("./Component"), {
  loading: () => <Skeleton />,
});
```

### Step 5.2: Error Boundaries and Loading States

```typescript
// Error handling
const { data, isLoading, error } = useQuery({
  queryKey: ["movies"],
  queryFn: fetchMovies,
  retry: 3,
  retryDelay: 1000,
});

// Loading states
if (isLoading) return <MovieCardSkeleton />;
if (error) return <ErrorMessage />;
```

### Step 5.3: Accessibility Improvements

- ARIA labels and roles
- Keyboard navigation
- Screen reader support
- Focus management
- Semantic HTML structure

### Step 5.4: SEO Optimization

```typescript
// Next.js metadata
export const metadata: Metadata = {
  title: "Cinetron - Premium Movie Discovery",
  description: "Discover movies with AI-powered recommendations",
  keywords: "movies, cinema, TMDB, recommendations",
  openGraph: {
    title: "Cinetron Premium",
    description: "The future of movie discovery",
    images: ["/og-image.jpg"],
  },
};
```

**🎉 Phase 5 Complete!**

- ✅ Performance optimized
- ✅ Error handling implemented
- ✅ Accessibility features added
- ✅ SEO optimized
- ✅ Production ready

---

## 🚀 Deployment Guide

### Prerequisites

1. **TMDB API Key**: Get from [TMDB API](https://www.themoviedb.org/settings/api)
2. **Google Gemini API Key**: Get from [Google AI Studio](https://makersuite.google.com/app/apikey)
3. **Vercel Account**: Sign up at [vercel.com](https://vercel.com)

### Deployment Steps

#### 1. Prepare for Deployment

```bash
# Build the project locally to check for errors
npm run build

# Run ESLint to check for code quality issues
npm run lint

# Fix any build issues before deploying
```

#### 2. Deploy to Vercel

```bash
# Install Vercel CLI (optional)
npm i -g vercel

# Deploy using CLI or connect GitHub repository
vercel

# Or deploy via Vercel dashboard
# 1. Connect your GitHub repository
# 2. Configure environment variables
# 3. Deploy automatically
```

#### 3. Environment Variables Setup

In Vercel dashboard, add these environment variables:

```bash
# TMDB Configuration
NEXT_PUBLIC_TMDB_API_KEY=your_tmdb_api_key_here
NEXT_PUBLIC_TMDB_BASE_URL=https://api.themoviedb.org/3
NEXT_PUBLIC_TMDB_IMAGE_BASE_URL=https://image.tmdb.org/t/p

# Google Gemini AI
NEXT_PUBLIC_GEMINI_API_KEY=your_gemini_api_key_here

# Application Configuration
NEXT_PUBLIC_APP_URL=https://your-app-domain.vercel.app
NEXT_PUBLIC_APP_NAME=Cinetron
```

#### 4. Verify Deployment

- ✅ Check all pages load correctly
- ✅ Verify API calls work with production URLs
- ✅ Test responsive design on different devices
- ✅ Validate search functionality
- ✅ Check movie/TV show detail pages

---

## 🎯 Features Overview

### Core Features

- **🎬 Movie Discovery**: Browse trending, popular, and top-rated movies
- **📺 TV Show Discovery**: Explore popular TV series and shows
- **🔍 Universal Search**: Search across movies, TV shows, and people
- **📱 Responsive Design**: Perfect experience on all devices
- **🌙 Dark Mode**: Toggle between light and dark themes
- **⚡ Fast Performance**: Optimized with caching and lazy loading

### Premium Features

- **🎨 3D Effects**: Interactive particle backgrounds and floating elements
- **✨ Animations**: Smooth transitions and micro-interactions
- **🎯 AI Integration**: Smart recommendations and search
- **🔥 Real-time Data**: Always up-to-date movie information
- **🎭 Premium UI**: Glassmorphism and modern design patterns
- **🚀 Advanced Navigation**: Smart routing and state management

### Technical Features

- **⚡ Next.js 15**: Latest React framework with App Router
- **🔷 TypeScript**: Full type safety and IntelliSense
- **🎨 Tailwind CSS**: Utility-first styling with DaisyUI
- **📊 React Query**: Intelligent data fetching and caching
- **🗃️ Zustand**: Lightweight state management
- **🎬 Framer Motion**: Production-ready animations

---

## 🏗️ Technical Architecture

### Data Flow

```
User Interaction → Component → Custom Hook → API Client → TMDB API
                                    ↓
              Zustand Store ← Cache Layer ← Response Data
                                    ↓
              Component State ← React Query ← Formatted Data
                                    ↓
                              UI Update
```

### Component Hierarchy

```
App Layout
├── Navbar (Universal Search)
├── Page Components
│   ├── HomePage (Trending Movies)
│   ├── MoviesPage (Popular Movies)
│   ├── TVShowsPage (Popular TV Shows)
│   └── DetailPages (Movie/TV Details)
├── Reusable Components
│   ├── MovieCards (Multiple Variants)
│   ├── 3D Effects (Particles, Floating Elements)
│   └── UI Components (Buttons, Modals)
└── Footer
```

### State Management

- **Theme State**: Dark/light mode preferences
- **Modal State**: Search modal, movie modal states
- **Cache State**: API response caching
- **Search State**: Search queries and filters
- **User State**: Preferences and watchlists

---

## 🛠️ Development Commands

```bash
# Development
npm run dev          # Start development server with Turbopack
npm run build        # Build for production
npm run start        # Start production server
npm run lint         # Run ESLint

# Useful Development Tips
npm run dev -- --port 3001    # Run on custom port
npm run build && npm run start # Test production build locally
```

## 📝 Project Structure Explained

```
movies_app_2025/
├── 📁 src/
│   ├── 📁 app/                 # Next.js 15 App Router
│   │   ├── 📄 globals.css      # Global styles and CSS variables
│   │   ├── 📄 layout.tsx       # Root layout with providers
│   │   ├── 📄 page.tsx         # Homepage with hero and trending
│   │   ├── 📁 movies/          # Movies listing page
│   │   ├── 📁 tv-shows/        # TV shows listing page
│   │   ├── 📁 movie/[slug]/    # Dynamic movie details
│   │   └── 📁 tv/[slug]/       # Dynamic TV show details
│   ├── 📁 components/          # Reusable React components
│   │   ├── 📄 Navbar.tsx       # Enhanced navigation with search
│   │   ├── 📄 TrendingMovieCard.tsx    # Premium movie cards
│   │   ├── 📄 EnhancedMovieCard.tsx    # Alternative movie cards
│   │   ├── 📁 3d-effects/      # Interactive visual effects
│   │   └── 📁 sections/        # Page section components
│   ├── 📁 lib/                 # Utility functions and configurations
│   │   ├── 📄 tmdb-client.ts   # TMDB API client
│   │   └── 📄 slug-utils.ts    # URL slug utilities
│   ├── 📁 hooks/               # Custom React hooks
│   │   └── 📄 index.ts         # Movie/TV data fetching hooks
│   ├── 📁 stores/              # Zustand state management
│   │   └── 📄 index.ts         # Theme, modal, and cache stores
│   └── 📁 types/               # TypeScript type definitions
│       └── 📄 index.ts         # API response and UI types
├── 📄 package.json             # Dependencies and scripts
├── 📄 tailwind.config.ts       # Tailwind CSS configuration
├── 📄 next.config.js           # Next.js configuration
├── 📄 tsconfig.json            # TypeScript configuration
└── 📄 .env.local               # Environment variables
```

---

## 🐛 Troubleshooting

### Common Issues

#### 1. TMDB API Key Issues

```bash
# Error: 401 Unauthorized
# Solution: Check your TMDB API key in .env.local
NEXT_PUBLIC_TMDB_API_KEY=your_actual_api_key_here
```

#### 2. Build Errors

```bash
# Type errors during build
npm run lint                    # Check for linting issues
npx tsc --noEmit               # Check TypeScript errors
```

#### 3. Environment Variables Not Loading

```bash
# Restart development server after adding env vars
npm run dev
```

#### 4. Styling Issues

```bash
# Clear Next.js cache
rm -rf .next
npm run dev
```

### Performance Tips

1. **Image Optimization**: Always use Next.js `Image` component
2. **Lazy Loading**: Implement intersection observer for below-fold content
3. **Caching**: Utilize React Query caching strategies
4. **Bundle Size**: Monitor with `npm run build` output

---

## 📈 Future Enhancements

### Planned Features

- **🤖 AI Chat**: Movie recommendation chatbot
- **👥 Social Features**: User reviews and ratings
- **🎮 Interactive Games**: Movie quizzes and trivia
- **📱 PWA Support**: Offline functionality
- **🔔 Push Notifications**: New releases and recommendations
- **🎪 Watch Parties**: Synchronized viewing experiences

### Technical Improvements

- **🔍 Advanced Search**: Filters and faceted search
- **📊 Analytics**: User behavior tracking
- **🔐 Authentication**: User accounts and profiles
- **💾 Database**: User data persistence
- **🚀 Performance**: Further optimizations

---

## 📄 License & Credits

### License

This project is private and proprietary.

### Credits & Acknowledgments

- **TMDB API**: Movie and TV show data
- **Google Gemini**: AI-powered features
- **Next.js Team**: Amazing React framework
- **Tailwind CSS**: Utility-first CSS framework
- **Vercel**: Deployment and hosting platform

### Developer

**Ameer Hasan**

- GitHub: [@Ameerusa86](https://github.com/Ameerusa86)
- LinkedIn: [Ameer Hasan](https://www.linkedin.com/in/ameerdev/)

---

<div align="center">

**🎬 Built with ❤️ using Next.js 15, TypeScript, and modern web technologies**

[![GitHub](https://img.shields.io/badge/GitHub-Ameerusa86-black?style=flat-square&logo=github)](https://github.com/Ameerusa86)
[![LinkedIn](https://img.shields.io/badge/LinkedIn-AmeerHasan-blue?style=flat-square&logo=linkedin)](https://www.linkedin.com/in/ameerdev/)

</div>
NEXT_PUBLIC_CLERK_AFTER_SIGN_IN_URL=/
NEXT_PUBLIC_CLERK_AFTER_SIGN_UP_URL=/
```

### Deployment Steps

1. **Connect Repository**: Link your GitHub repository to Vercel
2. **Configure Environment Variables**: Add all the environment variables listed above
3. **Deploy**: Vercel will automatically build and deploy your application

### Build Verification

The application has been tested and verified to build successfully:

```bash
npm run build  # ✅ Builds successfully
npm run lint   # ✅ No ESLint warnings or errors
```

## 🔑 API Keys Required

### TMDB API Key

1. Visit [TMDB API](https://www.themoviedb.org/settings/api)
2. Create an account and request an API key
3. Add the key to `NEXT_PUBLIC_TMDB_API_KEY`

### Google Gemini API Key

1. Visit [Google AI Studio](https://makersuite.google.com/app/apikey)
2. Create a new API key
3. Add the key to `NEXT_PUBLIC_GEMINI_API_KEY`

### Clerk Authentication

1. Visit [Clerk Dashboard](https://dashboard.clerk.com/)
2. Create a new application
3. Copy the publishable key and secret key
4. Add them to the respective environment variables

## 🏗️ Local Development

```bash
# Install dependencies
npm install

# Run development server
npm run dev

# Build for production
npm run build

# Start production server
npm start
```

## 📱 Features Overview

- **Home Page**: Trending movies and personalized recommendations
- **Discover**: Advanced filtering and search capabilities
- **Profile**: User watchlists, favorites, and viewing history
- **Games**: Interactive movie-related games and quizzes
- **AI Chat**: Movie recommendations powered by Google Gemini

## 🎨 Design System

- Premium glassmorphism effects
- Responsive design for all screen sizes
- Dark theme with orange accent colors
- Smooth animations and transitions
- Accessibility-focused components

## 📄 License

This project is private and proprietary.
